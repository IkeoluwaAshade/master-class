import React from 'react'

const Card = () => {
  return (
    <div>
        {/* ======================= SHORT NOTE ======================= */}

        { /* 
        The block folder is where all the reusable components are. For instace if I want to apply card.jsx in my other part of the folder, then I can reference them from here (i.e the block folder)
         */}
    </div>
  )
}

export default Card